# Spam Email Classifier :email:

- Uses pandas to process the csv into a dataframe readable by sklearn
- I chose to use a TfidfVectorizer as it uses weights to give more emphasis on key words in the dataset, which I think applies well to a spam email dataset.
s